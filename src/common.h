#pragma once
#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

/* Rust names are much nicer. */
typedef uint64_t    u64;
typedef uint32_t    u32;
typedef uint8_t      u8;

typedef int64_t     i64;
typedef int32_t     i32;
typedef int8_t       i8;

typedef size_t      usize;
